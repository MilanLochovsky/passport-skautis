/**
 * Module dependencies.
 */
var SkautISStrategy = require('./SkautISStrategy');


/**
 * Expose `Strategy` directly from package.
 */
exports = module.exports = SkautISStrategy;

/**
 * Export constructors.
 */
exports.Strategy = SkautISStrategy;
